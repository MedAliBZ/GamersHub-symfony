<!DOCTYPE html>

<title>New Spam</title>

{% block body %}
    <h1>Create new Spam</h1>

    {{ include('spam/_form.html.twig') }}

    <a href="{{ path('app_spam_index') }}">back to list</a>
{% endblock %}
