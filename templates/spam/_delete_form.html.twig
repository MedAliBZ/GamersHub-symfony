<form method="post" action="{{ path('app_spam_delete', {'id': spam.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spam.id) }}">
    <button class="btn">Delete</button>
</form>
