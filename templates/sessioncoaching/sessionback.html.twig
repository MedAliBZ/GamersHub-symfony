{% extends 'backBase.html.twig' %}
{% block title %}Coaching Session{% endblock %}
{% block extraStyle %}
    <link href="{{ asset('back/vendors/datatables.net-bs/css/dataTables.bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('back/vendors/datatables.net-buttons-bs/css/buttons.bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('back/vendors/datatables.net-fixedheader-bs/css/fixedHeader.bootstrap.min.css') }}"
          rel="stylesheet">
    <link href="{{ asset('back/vendors/datatables.net-responsive-bs/css/responsive.bootstrap.min.css') }}"
          rel="stylesheet">
    <link href="{{ asset('back/vendors/datatables.net-scroller-bs/css/scroller.bootstrap.min.css') }}"
          rel="stylesheet">{% endblock %}
{% block content %}
    <div class="col-md-12 col-sm-12" style="margin-top: 20px;">
        <div class="x_panel">
            <div class="x_title">
                <h2>Coaching Session TABLE</h2>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                <div class="row">
                    <div class="col-sm-12">
                        <div class="card-box table-responsive">
                            <p class="text-muted font-13 m-b-30">

                            </p>
                            <table id="datatable" class="table table-striped table-bordered" style="width:100%">
                                <thead>
                                <tr>
                                    {# <th>Id</th> #}
                                    <th>Username</th>
                                    {# <th>Name</th> #}
                                    {# <th>Second Name</th> #}
                                    <th>Price</th>
                                    <th>Start Day</th>
                                    <th>End Day</th>
                                    <th>Client</th>
                                    <th>Description</th>

                                    <th></th>
                                </tr>
                                </thead>
                                <tbody>
                                {% if(sessioncoachings is not null) %}
                                    {% for s in sessioncoachings %}
                                        <tr class="odd parent">
                                            {# <td>{{ usr.id }}</td> #}
                                            <td>{{ s.coach.user.username }}</td>
                                            {# <td>{{ usr.name }}</td> #}
                                            {# <td>{{ usr.secondName }}</td> #}
                                            <td>{{ s.prix}}</td>
                                            <td>{{ s.getDateDebut()|date }}</td>
                                            <td>{{ s.getDateFin()|date }}</td>
                                            <td>{{ s.user.username}}</td>
                                            <td>{{ s.description }}</td>
                                            <td>
                                                <a href="{{ path("session_edit_back", {'id': s.id}) }}"><i
                                                            class="fa fa-pencil"></i></a>
                                                <a href="{{ path("session_delete_back", {'id': s.id}) }}"><i
                                                            class="fa fa-trash-o"></i></a>
                                                </a>
                                            </td>
                                        </tr>
                                        <div class="modal fade" id="{{ s.coach.user.username }}" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                                            <div class="modal-dialog" role="document">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h4 class="modal-title" id="exampleModalLabel">Coaching Session</h4>
                                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                                            <span aria-hidden="true">&times;</span>
                                                        </button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <p><span style="font-size: 15px;font-weight: bold">Id: </span><span>{{ s.id }}</span></p>
                                                        <p><span style="font-size: 15px;font-weight: bold">Username: </span><span>{{ s.coach.user.username }}</span></p>
                                                        <p><span style="font-size: 15px;font-weight: bold">Prix: </span><span>{{ s.prix }}</span></p>
                                                        <p><span style="font-size: 15px;font-weight: bold">Start Day: </span><span>{{ s.getDateDebut()|date }}</span></p>
                                                        <p><span style="font-size: 15px;font-weight: bold">End Day: </span><span>{{ s.getDateFin()|date }}</span></p>
                                                        <p><span style="font-size: 15px;font-weight: bold">Client: </span><span>{{ s.user.username  }}</span></p>
                                                        <p><span style="font-size: 15px;font-weight: bold">Description: </span><span>{{ s.description }}</span></p>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    {% endfor %}
                                {% endif %}

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

