<form method="post" action="{{ path('rewards_delete', {'id': reward.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reward.id) }}">
    <button class="btn">Delete</button>
</form>
